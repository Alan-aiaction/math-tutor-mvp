# 2026-08-12 — Alan — Claude Code

- **Model:** Claude Sonnet 5 (`claude-sonnet-5`)
- **Notes:** Session covering: added a CLAUDE.md rule per direct request - a confirmed
  missing tool/capability gets surfaced immediately and asked about (both that it's broken
  and how to fix it), not silently built around with the gap only flagged at the end.
  Already saved to personal memory, but the user explicitly wanted it in CLAUDE.md too
  since it's read automatically every session, rather than relying on memory recall alone.
  Prompted by the Supabase MCP disconnection during ticket #68 - the migration got written
  and everything built around its absence before the user had to ask directly why I hadn't
  just asked him to reconnect it.
