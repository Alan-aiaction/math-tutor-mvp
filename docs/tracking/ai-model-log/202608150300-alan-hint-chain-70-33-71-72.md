# 2026-08-15 — Alan — Claude Code

- **Model:** Claude Sonnet 5 (`claude-sonnet-5`)
- **Notes:** Overnight run covering tickets #70, #33, #71, #72 — the full misconception →
  hint chain, built in one continuous session per explicit direction to do all four without
  check-ins in between.

  Two real decisions surfaced during planning, resolved before any code was written:

  1. **#70 hand-authored, not LLM-drafted.** `LLM_API_KEY` isn't configured anywhere in
     `backend/.env` (checked directly - no `LLM_*` vars present, despite `.env.example`
     documenting them). This repo has never made a real LLM call on any ticket so far - #67
     and #69 were only ever tested against a mocked provider. Rather than block the whole
     chain, hand-authored 14 hint variants (7 misconceptions × 2) directly instead, per
     explicit direction - same bootstrap-deviation pattern already used for #9's
     misconception rules.
  2. **How #71 detects a retry.** `/attempts/check` was fully stateless before this - no
     retry concept existed anywhere, and `attempt_steps` has no step-index column to
     correlate historical DB rows by. Chose to track retry count client-side
     (`frontend/app/page.js`'s new `wrongTryCounts` state) rather than infer it from
     persisted attempt history - full options table in `decision-log.md`.

  Built, in dependency order:
  - **#70** - `docs/architecture/hint_variants_bootstrap_batch_1.md`, drafted, not seeded.
  - **#33** - `backend/hint_selection.py` (new), wired `misconception_matching.py`'s
    standalone engine into `orchestration.py` for the first time (it was never wired in
    before this, despite existing since #30/#31).
  - **#71** - `backend/hint_escalation.py` (new, pure trigger logic), `EvaluationResult`
    gains `hint_level` (models.py, the agreed-contract file, extended deliberately and
    flagged), frontend retry-count plumbing.
  - **#72** - `backend/hint_validation.py` + `backend/hint_escalation_llm.py` (new),
    implementing #73's checklist's own automatable-check table, fail-safe to the generic
    hint on any failure (validation, missing key, network) - safe to ship with no real LLM
    key configured yet.

  Shipped as a 4-PR stack (#105 → #106 → #107 → #108), each branched off the previous
  ticket's branch rather than `master`, since each needed the previous one's code physically
  present and nothing was being merged mid-run. Flagged prominently in every PR description
  and the decision-log entry that these must merge bottom-up, retargeting each base to
  `master` as the one below it merges - the exact stacked-PR trap CLAUDE.md already
  documents.

  Full backend suite grew 145 → 156 → 171 → 183 → 204 across the four tickets, zero
  regressions at any step. Frontend changes (question_text passthrough, wrongTryCounts
  tracking) could not be live-tested - no one available to run the dev UI overnight - noted
  honestly in each PR rather than claimed as verified.

  Two things still need a human: explicit approval on #70's 14 drafted hints before seeding
  (same gate every content batch in this project has used), and a real `LLM_API_KEY` before
  #72 ever produces a live hint for real - it degrades safely to the generic hint until then.
