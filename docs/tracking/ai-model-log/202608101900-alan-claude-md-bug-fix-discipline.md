# 2026-08-10 — Alan — Claude Code

- **Model:** Claude Sonnet 5 (`claude-sonnet-5`)
- **Notes:** Session covering: added a new CLAUDE.md rule under "Working style" per direct
  correction - a confirmed real bug (KaTeX rendering) got fully described but then three
  unrelated doc/process changes were done first before the actual fix, and the user had to
  ask directly why the bug still wasn't fixed. New rule: fix confirmed bugs before other
  requested work, plan the fix (root cause + approach) and get explicit approval before
  coding, and name the saved plan file after the ticket that introduced the bug rather than
  a fresh number.
